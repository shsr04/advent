# Day2 hasRepeatingDigits Spec Update Prompt

Read the updated `day2/day2-spec.md` and implement compiler support required for the specified `hasRepeatingDigits` style while keeping the compiler generic (no domain-specific branches).

Deliverables:
1. Extend parser/type/codegen/runtime features needed by the updated spec.
2. Keep behavior generic and reusable for other programs.
3. Add/adjust compiler tests under `compiler/tests/` for new syntax/semantics.
4. Build and run day2 (`.metac -> .c -> binary`) and report whether it works.

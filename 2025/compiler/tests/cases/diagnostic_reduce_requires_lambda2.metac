function main() {
  const result = solve()?
  log("Result: ${result}")
}

function solve(): number | error {
  const digits = "12".chars().map(parseNumber)?
  const value = digits.reduce(0, x => x)
  return value
}

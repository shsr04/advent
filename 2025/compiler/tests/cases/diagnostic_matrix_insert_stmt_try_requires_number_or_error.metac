@Test({
   "compile_err" : "Unsupported try expression in const assignment"
})
function main() {
  const result = run()
  log("Result: ${result}")
}

function run(): number {
  max(1, 2)?
  return 0
}

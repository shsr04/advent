@Test({
   "exit" : 0,
   "stdout" : "1-0\n"
})
function main() {
  let xs: bool[] = []
  xs.push(true)
  xs.push(false)

  const ys = xs
  if ys.size() == 2 {
    const [a, b] = ys
    log("${a}-${b}")
  }
}

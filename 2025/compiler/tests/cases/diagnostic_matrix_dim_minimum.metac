@Test({
   "compile_err" : "matrix dim(...) must be at least 2"
})
function main() {
  const result = run()?
  log("Result: ${result}")
}

function run(): number | error {
  let grid: matrix(number) with dim(1) = []
  return grid.members().size()
}

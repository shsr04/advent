function main() {
  const result = solve()?
  log("Result: ${result}")
}

function solve(): number | error {
  const parts = split(STDIN, "-")?
  if parts.size() != 2 {
    return error("Invalid range")
  }
  const [start, end] = parts

  for const x in seq(start, end) {
    return x
  }

  return 0
}

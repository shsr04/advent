@Test({
   "compile_err" : "Builtin 'log' does not support argument type 'bool | number'"
})
function main() {
  const v = tri()?
  log(v)
}

function tri(): number | bool | error {
  return error("boom")
}

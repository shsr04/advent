function rhs(): boolean | error {
  const caps = "x".match("[")?
  return caps.size() > 0
}

@Test({
   "compile_err" : "Postfix '?' is only supported"
})
function main(): int {
  if true || rhs()? {
    return 0
  }
  return 1
}

@Test({
   "exit" : 0,
   "stdout" : "Result: 42\n"
})
function main() {
  const result = solve()?
  log("Result: ${result}")
}

function solve(): number | error {
  const n = parseNumber("42")?
  return n
}

function main() {
  const result = solve()?
  log("Result: ${result}")
}

function solve(): number | error {
  let n = 42
  const label = "n=${n}"

  if label == "n=42" {
    return 1
  } else {
    return 0
  }
}

@Test({
   "compile_err" : "Empty list literal requires an explicit list type"
})
function main() {
  const result = run()?
  log("Result: ${result}")
}

function run(): number | error {
  let values = []
  return 0
}

@Test({
   "compile_err" : "Method 'insert(...)' on 'number_list' requires compile-time in-bounds proof"
})
function main(): int {
  const nums: int[] with size(3) = [1, 2, 3]
  const i = 1
  const next = nums.insert(7, i)
  return next[0]
}

function bad(m: matrix(number)): int {
  const around = m.neighbours(0)
  return around.size()
}

@Test({
   "compile_err" : "requires value with source matrix-member metadata"
})
function main(): int {
  return 0
}

function sumThree(xs: int[]): int {
  const h = xs.head()
  const l = xs.last()
  const m = xs[0]
  return h + l + m
}

@Test({
   "compile_err" : "Unsupported method call 'head' on type 'number_list'"
})
function main(): int {
  return sumThree([1, 2, 3])
}

@Test({
   "compile_err" : "Conditional comparison in while condition depends only on immutable values"
})
function main() {
  const result = solve()?
  log("Result: ${result}")
}

function solve(): number | error {
  const limit = 3
  while limit > 0 {
    return 1
  }

  return 0
}

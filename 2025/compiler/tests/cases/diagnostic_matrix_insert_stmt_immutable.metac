@Test({
   "compile_err" : "Cannot mutate immutable variable 'grid'"
})
function main() {
  const result = run()?
  log("Result: ${result}")
}

function run(): number | error {
  const grid: matrix(number) with dim(2), matrixSize([2, 2]) = []
  grid.insert(1, [0, 0])
  return 0
}

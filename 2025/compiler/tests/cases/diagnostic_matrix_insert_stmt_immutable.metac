function main() {
  printf("Result: %d\n", run() or (e) => {
    printf("Error! %s\n", e.message)
    return 1
  })
}

function run(): number | error {
  const grid: matrix(number) with dim(2), matrixSize([2, 2]) = []
  grid.insert(1, [0, 0])
  return 0
}

@Test({
   "compile_err" : "Unsupported interpolation expression type: bool | number"
})
function main() {
  const result = run()
  log("Result: ${result}")
}

function run(): number | bool {
  return 1
}

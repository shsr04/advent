@Test({
   "compile_err" : "Method 'size(...)' requires compile-time axis proof in range(0, 2)"
})
function main(): int {
  let grid: matrix(number) with dim(3) + matrixSize([2, 3, 4]) = []
  let axis: int with range(0, 2) = 1
  axis = 3
  return grid.size(axis)
}

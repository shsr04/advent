@Test({
   "compile_err" : "Operator '+' requires number operand, got bool | number"
})
function main() {
  const x: number | bool = pick()
  const y = x + 1
  log("Result: ${y}")
}

function pick(): number | bool {
  return 4
}

@Test({
   "compile_err" : "requires compile-time in-bounds proof"
})
function main(): int {
  const nums: int[] with size(3) = [4, 5, 6]
  let sum: int = 0

  for const i in seq(0, 2) {
    sum = sum + nums[i]
  }

  if sum == 15 {
    return 0
  }
  return 1
}

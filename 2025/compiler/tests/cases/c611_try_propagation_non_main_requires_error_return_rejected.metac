function helper(): int {
  const parts = "x".match("[")?
  return parts.size()
}

@Test({
   "compile_err" : "error propagation needs `error` return type"
})
function main(): int {
  return helper()
}

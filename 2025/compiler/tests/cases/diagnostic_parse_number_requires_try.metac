@Test({
   "compile_err" : "parseNumber(...) is fallible; use parseNumber(...)? or map(parseNumber)?"
})
function main() {
  const result = solve()?
  log("Result: ${result}")
}

function solve(): number | error {
  const n = parseNumber("42")
  return n
}

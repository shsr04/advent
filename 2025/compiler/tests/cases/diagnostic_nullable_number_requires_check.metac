@Test({
   "compile_err" : "Operator '+' requires number operand, got number_or_null"
})
function main() {
  const result = run()?
  log("Result: ${result}")
}

function run(): number | error {
  let maybe: number | null = null
  let next = maybe + 1
  return next
}

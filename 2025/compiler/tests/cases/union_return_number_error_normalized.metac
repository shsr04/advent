@Test({
   "exit" : 0,
   "stdout" : "Result: 5\n"
})
function main() {
  const result = run()?
  log("Result: ${result}")
}

function helper(): number | error | number {
  return 5
}

function run(): error | number {
  const v = helper()?
  return v
}

@Test({
   "exit" : 0,
   "stdout" : ""
})
function main(): int {
  const a: number with range(0, 10) = 5
  const b: number with range(0, *) = a
  const c: number = b

  if c == 5 {
    return 0
  }
  return 1
}

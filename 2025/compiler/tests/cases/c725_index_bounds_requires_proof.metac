@Test({
   "compile_err" : "Index on 'number_list' requires compile-time in-bounds proof"
})
function main() {
  const result = probe()?
  log("Result: ${result}")
}

function probe(): number | error {
  const nums = split("10,20", ",")?.map(parseNumber)?
  let idx = 0
  return nums[idx]
}
